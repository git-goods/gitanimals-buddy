{
  "hooks": [
    {
      "event": "SessionStart",
      "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/fetch-pet.sh && bash ${CLAUDE_PLUGIN_ROOT}/scripts/fetch-usage.sh && bash ${CLAUDE_PLUGIN_ROOT}/scripts/sync-commands.sh",
      "description": "Fetch pet data, usage stats, and sync slash commands on session start"
    }
  ]
}
