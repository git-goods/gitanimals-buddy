{
  "hooks": [
    {
      "event": "SessionStart",
      "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/fetch-pet.sh && bash ${CLAUDE_PLUGIN_ROOT}/scripts/fetch-usage.sh",
      "description": "Fetch pet data and usage stats on session start"
    }
  ]
}
